{
  "name": "house-clean",
  "version": "1.0.0",
  "description": "House cleaning services website",
  "scripts": {
    "build": "cd frontend && npm ci && npm run build",
    "dev": "cd frontend && npm run dev",
    "start": "cd backend && npm start"
  },
  "dependencies": {
    "nodemailer": "^7.0.5"
  }
}
